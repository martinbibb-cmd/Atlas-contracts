/**
 * ids.ts
 *
 * Shared ID type utilities for Atlas contracts.
 *
 * All entity identifiers are opaque strings (UUID recommended).  These type
 * aliases document intent and allow future refinement without breaking changes.
 */

/** An opaque string identifier (UUID recommended). */
export type AtlasId = string;
