/**
 * versions.ts
 *
 * Legacy module — retained only for compatibility shims.
 *
 * The canonical scan handoff contract is SessionCaptureV1.
 * New code must not reference ScanBundleV1 or ScanBundleVersion.
 *
 * @deprecated  Use SessionCaptureV1 (src/atlasScan/sessionCaptureV1.types.ts)
 *              as the canonical scan handoff contract.
 */
